import { Checker } from "./lib/checker/checker";
import { ILoaderOption } from "./lib/loader/loader.types";
export declare class Difficulty extends Checker {
    constructor(wordlist: string[][]);
}
export declare function create(option?: ILoaderOption): Promise<Difficulty>;
export declare function createSync(option?: ILoaderOption): Difficulty;
