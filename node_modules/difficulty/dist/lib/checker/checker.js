"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Checker = /** @class */ (function () {
    function Checker(wordList) {
        this.wordList = wordList;
    }
    Checker.prototype.getLevel = function (word) {
        var _this = this;
        // Default is super hard :O
        var level = this.wordList.length - 1;
        this.wordList.forEach(function (levelWordList, index) {
            if (levelWordList.indexOf(word) > -1) {
                level = index === 0 ? 0 : (_this.wordList.length - index);
            }
        });
        return level;
    };
    return Checker;
}());
exports.Checker = Checker;
