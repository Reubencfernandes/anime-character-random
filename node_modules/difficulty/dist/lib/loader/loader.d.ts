import { ILoaderOption } from "./loader.types";
export declare class Loader {
    static loadCSV(option?: ILoaderOption): Promise<string[][]>;
    static loadCSVSync(option?: ILoaderOption): string[][];
    private static transformCSVBufferToWordList;
    private static getLevel;
}
